<UserControl x:Class="OpenCoreBuilder.UI.Views.AboutView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <ScrollViewer>
            <StackPanel>
                <TextBlock Text="About"
                        FontSize="20"
                        FontWeight="Bold"
                        Margin="0,0,0,20"/>

                <TextBlock Text="OpenCore Builder"
                        FontSize="16"
                        FontWeight="Bold"/>
                <TextBlock Text="Author: mhmdwaelanwr"
                        Margin="0,5,0,20"/>

                <TextBlock Text="Social Links"
                        FontSize="16"
                        FontWeight="Bold"
                        Margin="0,0,0,10"/>

                <ItemsControl ItemsSource="{Binding SocialLinks}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Button Content="{Binding Platform}"
                                    Command="{Binding DataContext.OpenLinkCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                    CommandParameter="{Binding Url}"
                                    Margin="0,2"
                                    HorizontalAlignment="Left"
                                    Width="200"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
